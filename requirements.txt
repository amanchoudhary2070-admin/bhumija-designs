Django>=5.2,<6.0
Pillow>=10.0          # product photos (ImageField)
requests>=2.31         # calls the Razorpay REST API
python-dotenv>=1.0    # loads .env in development
whitenoise>=6.6       # serves static files in production
gunicorn>=22.0        # production web server
dj-database-url>=2.1  # DATABASE_URL support
psycopg[binary]>=3.1  # Postgres driver, needed once DATABASE_URL is set (e.g. on Render/Railway)
